# 构建 Windows 单文件程序

在项目目录中右键打开 PowerShell，然后执行：

```powershell
powershell -ExecutionPolicy Bypass -File .\build-exe.ps1
```

不要直接运行 `node .\tools\build-exe.js`，它只负责生成约 `94 MB` 的原始 SEA。构建脚本会在 PowerShell 中输出各阶段文字，并在 UPX 压缩期间显示进度条；请等待输出 `Build completed` 和 `Done`，之后 `dist\LAN_CHAT.exe` 才是约 `23 MB` 的最终压缩版。

构建成功后会生成：

```text
dist\LAN_CHAT.exe
```

当前 Windows x64 构建实测由约 `93.8 MB` 压缩到约 `23.2 MB`，具体体积会随 Node.js 和 UPX 版本变化。

最终用户只需要这个 EXE 文件，不需要安装 Node.js、npm 或其他运行环境。

构建脚本默认使用经过 SHA-256 校验的官方 UPX 压缩完整 Node.js 运行时，并在完成后执行 UPX 完整性测试。页面仍内嵌在唯一的 `server.js` 中；构建时会临时拆出资源并生成不含页面副本的 SEA 入口，避免重复嵌入。压缩通过后会自动删除原始 SEA 和全部临时入口，只保留 `dist` 中的最终文件。若需要生成未经 UPX 压缩的最终文件，请执行：

```powershell
powershell -ExecutionPolicy Bypass -File .\build-exe.ps1 -SkipCompression
```

双击 `LAN_CHAT.exe` 后，控制台会显示本机和局域网访问地址。关闭控制台窗口即可停止聊天服务。

## 当前聊天功能

- 提供 5 个公共聊天频道，默认名称为“频道1”至“频道5”。
- 支持公共频道和在线用户一对一私聊。
- 支持回复和引用消息，点击引用可跳转并高亮当前已加载的原消息；也可点击发送者名称快速插入 `@用户名`，被提及的消息会醒目标记。
- 公共频道会显示未读消息数量；消息支持回复、复制和撤回。
- 支持用户主动开启桌面通知，页面在后台收到消息时会显示系统通知，同时浏览器标签标题会稳定显示 `LAN CHAT · 未读消息 N`；网站图标保持不变，不进行闪烁。
- 用户名保存在当前设备的浏览器本地存储中，桌面端和移动端再次进入时会优先恢复；没有记录时自动生成随机名称。头像使用昵称首字符，并从 50 组稳定渐变色中按昵称分配。
- 公共频道和双方在线的私聊都可以发送文件，默认单文件上限为 1 MB。
- 文件可以通过回形针按钮、拖放到聊天区域或在输入框中粘贴剪贴板文件来发送，一次处理一个文件。
- 不超过 25 MB 的 JPEG、PNG、GIF、WebP、AVIF 和 BMP 图片会在消息气泡中直接显示，点击可打开全屏预览；SVG 和其他文件继续使用下载卡片。
- 公共频道文件使用临时磁盘分片中转；不超过 64 MB 的私聊文件优先尝试 WebRTC 点对点直传，失败后自动回退服务器。
- 频道文件仅当前频道用户可以下载，私聊文件仅双方可以下载。
- 消息与文件默认保留 10 分钟，管理员可实时调整；撤回文件消息时立即删除。
- 管理员工具箱支持手动输入限制，可将销毁时间实时设置为 1～1440 分钟内的整数，并将单文件上限设置为 1～20480 MB（20 GB）内的整数。
- 限制输入框仅允许输入数字；管理员密码框支持闪烁光标和 Enter 快捷验证。
- 服务器中转采用 1 MB 分片、SHA-256 校验、自动重试和断点续传；暂停会立即中断当前分片并在继续后重传，取消可在写入或最终校验期间生效；单次上传最长允许持续 12 小时。
- 私聊提供正在输入、已发送、已送达、已读、离线和文件接收状态。
- 使用浅色 iOS 26 液态玻璃视觉和纯 CSS 网格背景；三列主布局透明无框，消息气泡在桌面端与移动端均按内容自适应，最长占满头像旁的可用宽度。
- 管理员工具箱内部三列使用纯透明布局以降低渲染开销；管理员验证、工具箱和用户信息弹窗均支持点击外部空白处关闭。
- 管理员设置会同步给所有在线用户，并在当前服务进程中实时生效；服务重启后恢复为 `server.js` 内置默认值。
- 文字和文件共用“每 5 秒只能发送 2 条消息”的频率限制。

## 注意事项

- 默认端口为 `9000`，如果端口已被其他程序占用，程序会启动失败。
- 服务启动成功后会自动使用系统默认浏览器打开本地访问地址。
- 可以在启动前设置 `LAN_CHAT_PORT` 环境变量来更换端口。
- 如需后台运行而不自动打开浏览器，可将 `LAN_CHAT_NO_OPEN` 设置为 `1`。
- 管理员默认密码仍为 `666666`。正式分发前建议通过 `ADMIN_PASSWORD` 环境变量修改。
- 当前 EXE 未进行数字签名，Windows 首次运行时可能显示安全提示。
- UPX 能显著减小文件体积，但未经数字签名的压缩程序可能触发部分安全软件的启发式提示；正式公开分发建议进行代码签名。
- Windows x64 和其他操作系统需要分别构建对应的程序。
