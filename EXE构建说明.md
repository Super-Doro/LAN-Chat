# 构建 Windows 单文件程序

在项目目录中右键打开 PowerShell，然后执行：

```powershell
powershell -ExecutionPolicy Bypass -File .\build-exe.ps1
```

构建成功后会生成：

```text
dist\VOID-Chat.exe
```

最终用户只需要这个 EXE 文件，不需要安装 Node.js、npm 或其他运行环境。

双击 `VOID-Chat.exe` 后，控制台会显示本机和局域网访问地址。关闭控制台窗口即可停止聊天服务。

## 当前聊天功能

- 支持公共频道和在线用户一对一私聊。
- 公共频道不允许发送文件。
- 私聊双方在线时可以发送最大 1 MB 的文件。
- 文件采用临时磁盘流式中转，不会将完整文件保存在服务端内存中。
- 私聊文件仅双方可以下载，5 分钟后自动销毁；撤回文件消息时立即删除。
- 文字和文件共用“每 5 秒只能发送 2 条消息”的频率限制。

## 注意事项

- 默认端口为 `9000`，如果端口已被其他程序占用，程序会启动失败。
- 可以在启动前设置 `VOID_CHAT_PORT` 环境变量来更换端口。
- 管理员默认密码仍为 `666666`。正式分发前建议通过 `ADMIN_PASSWORD` 环境变量修改。
- 当前 EXE 未进行数字签名，Windows 首次运行时可能显示安全提示。
- Windows x64 和其他操作系统需要分别构建对应的程序。
